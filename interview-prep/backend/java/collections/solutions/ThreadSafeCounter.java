import java.util.concurrent.ConcurrentHashMap;

/**
 * Challenge 3: Thread-Safe Counter Map
 * 
 * 1. Use ConcurrentHashMap with compute() or merge()
 * 2. increment(String key) — thread-safe increment
 * 3. topK(int k) — return top K keys by count
 * 4. Test with 10 threads incrementing concurrently
 */
public class ThreadSafeCounter {

    // YOUR CODE HERE

    public static void main(String[] args) throws InterruptedException {
        // Test concurrent increments, verify no lost updates
    }
}
