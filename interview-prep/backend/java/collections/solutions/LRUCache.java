/**
 * Challenge 2: LRU Cache (without extending LinkedHashMap)
 * 
 * 1. Use HashMap + doubly-linked list (build manually)
 * 2. get(key) — return value, move to front
 * 3. put(key, value) — insert, evict LRU if at capacity
 * 4. All operations must be O(1)
 * 5. Test with capacity=3, insert 5 items, verify eviction
 */
public class LRUCache<K, V> {

    // YOUR CODE HERE

    public static void main(String[] args) {
        // Test LRU eviction behavior
    }
}
